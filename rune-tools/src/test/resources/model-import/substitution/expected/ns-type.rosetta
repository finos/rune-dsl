namespace test.ns : < "test.ns definition" >
version "${project.version}"

type Document:
    [rootType]

    animal AnimalBase (1..1)

type Cow extends AnimalBase:

type Goat extends AnimalBase:

type Fish extends AnimalBase:

type Shark extends Fish:

type Salmon extends Fish:

type AnimalBase:

    name string (0..1)
