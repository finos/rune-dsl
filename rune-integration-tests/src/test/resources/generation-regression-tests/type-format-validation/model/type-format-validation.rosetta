namespace test.typeformatvalidation

typeAlias PositiveInt:
    int

    condition IsPositive:
        item > 0

type Measurements:
    amount PositiveInt (1..1)
    note string (0..1)
    amounts PositiveInt (0..*)
