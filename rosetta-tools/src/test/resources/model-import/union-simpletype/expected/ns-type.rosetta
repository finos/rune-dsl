namespace test.ns : < "test.ns definition" >
version "${project.version}"

typeAlias Foo: string(minLength: 2, maxLength: 2, pattern: "([A-Z]{2})")

typeAlias Bar: string(minLength: 4, maxLength: 6, pattern: "([A-Z]{2}-[A-Z0-9]{1,3})")

choice FooBar:

    Foo
    Bar
